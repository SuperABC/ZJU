#include <original.h>

void Original::decrease(int index, int distance) {
	int queueIndex = identifier[index];
	while (queueIndex > 1 && queue[(queueIndex - 1) / 2] > queue[queueIndex]) {
		
	}
}
int Original::get() {

}